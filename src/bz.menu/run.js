define([
    'bz.menu/app',

    'bz.menu/directives/bzMenuNav',
    'bz.menu/factories/menu'
], function(app) {

    //app.config([function() {}]);

    //app.run([function() {}]);

    return app;
});