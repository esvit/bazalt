define([
    'bz.widgets/app',

    'bz.widgets/directives/widget',
    'bz.widgets/providers/bzWidget',
    'bz.widgets/providers/bzWidgets'
], function(app) {

    //app.config([function() {}]);

    //app.run([function() {}]);

    return app;
});